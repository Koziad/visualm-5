import { MaterialIngredient } from './material-ingredient';

describe('MaterialIngredient', () => {
  it('should create an instance', () => {
    expect(new MaterialIngredient()).toBeTruthy();
  });
});
